#!/bin/sh
# Prevent committing analytics.json by accident
if [ -f analytics.json ]; then
  echo "ERROR: analytics.json must not be committed. Remove it from the index before committing." >&2
  exit 1
fi
exit 0
